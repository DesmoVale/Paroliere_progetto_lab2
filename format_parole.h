// Rendo tutto maiuscola la stringa
void to_upper_case(char* str);

// Rimuovo la u per il corretto controllo della parola
char* remove_u(char *str);

void to_lower_case(char* str);

int verifica_username(char* str);